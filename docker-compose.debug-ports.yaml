services:
  spurline:
    ports:
      - "${SPURLINE_DEBUG_BIND_ADDRESS:-127.0.0.1}:${SPURLINE_DEBUG_PORT:-8780}:8080"

  grove:
    ports:
      - "${GROVE_DEBUG_BIND_ADDRESS:-127.0.0.1}:${GROVE_DEBUG_PORT:-8001}:8000"
